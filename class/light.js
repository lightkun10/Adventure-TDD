const { Item } = require('./item');

class Light extends Item {
    /**
     * Dark rooms show only a description of
     * "You cannot see anything" unless a light is
     * in the room or being held.
    */
}


module.exports = {
    Light,
};
